#
# Tests needing root, using sudo instead of super
#

mknod01 sudo mknod01
# Basic test for mknod(2)

setgroups01 sudo setgroups01
# Basic test for setgroups(2)

setuid02 sudo setuid02
# Basic test for setuid(2) as root

unlink08-root sudo unlink08
# unlink(2) negative testcases as root
