#!/bin/sh

# generated-tests/argument.2.test is part of the GNU m4 testsuite
# generated from example in m4.texinfo line 1064

#. ${srcdir}/defs

cat <<\EOF >in
define(`exch', `$2, $1')
define(exch(``expansion text'', ``macro''))
macro
EOF

cat <<\EOF >ok


expansion text
EOF

$M4 -d in >out

$CMP -s out ok

