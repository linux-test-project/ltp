#!/bin/sh

# generated-tests/pushdef.1.test is part of the GNU m4 testsuite
# generated from example in m4.texinfo line 1312

#. ${srcdir}/defs

cat <<\EOF >in
define(`foo', `Expansion one.')
foo
pushdef(`foo', `Expansion two.')
foo
popdef(`foo')
foo
popdef(`foo')
foo
EOF

cat <<\EOF >ok

Expansion one.

Expansion two.

Expansion one.

foo
EOF

$M4 -d in >out

$CMP -s out ok

