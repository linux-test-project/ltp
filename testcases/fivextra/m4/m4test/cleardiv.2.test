#!/bin/sh

# generated-tests/cleardiv.2.test is part of the GNU m4 testsuite
# generated from example in m4.texinfo line 2850

#. ${srcdir}/defs

cat <<\EOF >in
define(`cleardivert',
`pushdef(`_num', divnum)divert(-1)undivert($@)divert(_num)popdef(`_num')')
EOF

cat <<\EOF >ok

EOF

$M4 -d in >out

$CMP -s out ok

